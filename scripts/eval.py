"""Eval CLI — P0 task #8/#9, interface frozen, implementation pending.

Subcommands (target):

  python scripts/eval.py validate --cases tests/cases.jsonl
  python scripts/eval.py run      --cases tests/cases.jsonl [--category C,...] [--parallel N]
  python scripts/eval.py diff     .eval/run-A.json .eval/run-B.json

See README ("P0 spec") for output schema and acceptance criteria.
"""
from __future__ import annotations

# TODO(P0-8/9): implement validate / run / diff.
